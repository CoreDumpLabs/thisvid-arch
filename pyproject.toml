[project]
name = "thisvid-favs"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "requests",
    "python-dotenv",
    "yt-dlp",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
thisvid-arch = "thisvid_arch:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
