[project]
name = "thisvid-favs"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "requests",
    "python-dotenv",
]

[project.optional-dependencies]
dev = ["pytest"]

[project.scripts]
thisvid = "thisvid:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
