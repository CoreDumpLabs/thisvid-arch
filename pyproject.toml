[project]
name = "thisvid-arch"
version = "0.1.0"
requires-python = ">=3.8"
dependencies = [
    "requests",
    "python-dotenv",
    "yt-dlp",
    "camoufox",
    "playwright==1.50.0",
]

[project.optional-dependencies]
dev = ["pytest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
